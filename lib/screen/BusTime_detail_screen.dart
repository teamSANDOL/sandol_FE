import 'package:flutter/material.dart';

class BusTimeDetailScreen extends StatelessWidget {
  const BusTimeDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('버스 시간표 상세 조회 페이지 입니다.'),
      ),
    );
  }
}
